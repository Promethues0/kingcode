node_modules/
.kingcode/
.env
*.log
.DS_Store
__pycache__/

# 构建产物
mac/build/
win/bin/
win/obj/
win/publish/

# eval 运行产物（基线 eval/baseline.json 有意入库，不在此列）
eval/results/
eval/runs/

# rehearse-emulator.sh 的产物目录（截图与页面快照，不入库）
rehearsal-*/

# eval 树不设 KINGCODE_EVAL_SESSIONS_ROOT 时的兜底会话目录（eval/cordis.eval.yml）
.kingcode-eval/
